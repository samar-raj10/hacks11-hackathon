# Architecture

CAMPUSSHIELD is a MERN + FastAPI system. React is the user interface, Express is the primary API and authorization boundary, MongoDB stores operational data, Redis supports real-time/background workflows, Socket.IO pushes dashboard updates, and FastAPI performs explainable statistical analysis.
