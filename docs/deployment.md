# Deployment

Docker Compose runs MongoDB, Redis, backend, and analytics locally. The frontend is Vercel-ready, the backend is suitable for Render/Railway/AWS, and analytics can be deployed independently as a FastAPI service.
