# API Overview

Phase 1 implemented health checks only:
- `GET /health`
- `GET /api/health`

Authentication, reports, outbreak, analytics, simulation, advisory, and admin APIs are planned for later phases.
