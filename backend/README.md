# CAMPUSSHIELD Backend

Node.js + Express + TypeScript API for authentication, RBAC, reports, simulations, notifications, and analytics orchestration.

## Local setup

```bash
cp .env.example .env
npm install
npm run dev
```

Phase 1 exposes health checks and Socket.IO scaffolding. Domain APIs are planned for later phases.
